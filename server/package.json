{
  "name": "mulmocast-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config ../wrangler.jsonc",
    "deploy": "wrangler deploy --config ../wrangler.jsonc",
    "deploy-prod": "wrangler deploy --config ../wrangler.jsonc --name mulmocast-prod",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "4.13.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "~6.0.3",
    "wrangler": "^4.125.0"
  }
}
