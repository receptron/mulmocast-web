{
  "name": "mulmocast-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "web",
    "server"
  ],
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=web",
    "build:server": "npm run build --workspace=server",
    "deploy": "npm run config:dev --workspace=web && VITE_GTAG=GTM-N6QMN3VF npm run build --workspace=web && npm run deploy --workspace=server",
    "deploy-prod": "npm run config:prod --workspace=web && VITE_GTAG=GTM-W8L5QNPS npm run build --workspace=web && npm run deploy-prod --workspace=server && npm run config:dev --workspace=web",
    "lint": "npm run lint --workspace=web",
    "format": "prettier --write 'web/**/*.{ts,json,yaml,vue}' 'server/**/*.{ts,json}' '*.json' '*.md'"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
