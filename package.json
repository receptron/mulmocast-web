{
  "name": "mulmocast-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev": "yarn run dev --workspace=web",
    "dev:server": "yarn run dev --workspace=server",
    "build": "yarn run build --workspace=web",
    "build:server": "yarn run build --workspace=server",
    "deploy": "yarn run config:dev --workspace=web && VITE_GTAG=GTM-N6QMN3VF yarn run build --workspace=web && yarn run deploy --workspace=server",
    "deploy-prod": "yarn run config:prod --workspace=web && VITE_GTAG=GTM-W8L5QNPS yarn run build --workspace=web && yarn run deploy-prod --workspace=server && yarn run config:dev --workspace=web",
    "lint": "yarn run lint --workspace=web",
    "format": "prettier --write 'web/**/*.{ts,json,yaml,vue}' 'server/**/*.{ts,json}' '*.json' '*.md'"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
